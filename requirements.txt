# Core dependencies
click>=8.0.0
rich>=13.0.0
pyyaml>=6.0.0
pydantic>=2.0.0

# HTTP/Networking
requests>=2.31.0
aiohttp>=3.9.0

# AWS Bedrock (for agent features)
boto3>=1.34.0

# Docker (for sandbox auditing)
docker>=7.0.0

# API Server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
sse-starlette>=2.0.0

# SSL/TLS verification
cryptography>=42.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
